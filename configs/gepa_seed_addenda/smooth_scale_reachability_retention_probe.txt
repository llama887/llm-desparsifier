For common-solve efficiency, prefer smooth local ranking over large constants: keep heuristic terms roughly on the scale of plausible move counts, avoid 1e4/1e6-style penalties except for rule-proven deadlocks, and combine subgoal counts with distances or interaction costs so neighboring states do not collapse into broad plateaus. When RULES show staged mechanics such as switches, doors, carrying, beams, pulling, swapping, sliding, or terrain changes, preserve solve-relevant mechanics but make progress continuous across the stage transition instead of switching between unrelated cost regimes. If RULES, COLLISIONLAYERS, static objects, hazards, doors, terrain, blockers, or one-way effects make legal paths differ from Manhattan distance, preserve a finite local reachability/path-distance term inside that observable branch instead of replacing it with plain counts, Manhattan distance, or score fallback. Use score_normalized or score only as a small tie-breaker unless object/rule features are unreliable.
