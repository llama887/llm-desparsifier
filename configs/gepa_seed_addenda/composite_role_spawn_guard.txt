Before choosing heuristic features, read WINCONDITIONS to identify every required role, then scan LEGEND and RULES for aliases, composites, or transformations that make one object embody multiple required roles. Add positions of such composite objects to every role set they satisfy, so an object that is both crate-like and target-like contributes to both counts. Only after this role augmentation, if a required role still has zero instances and RULES contain no clause that can create or transform into that role, apply a very large deadlock or missing-object penalty. Otherwise use a modest score_normalized fallback or small distance-based term instead of assuming missing objects are permanent.
