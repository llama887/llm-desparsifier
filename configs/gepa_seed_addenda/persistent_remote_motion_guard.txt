For persistent failures, test one conservative mechanics-grounded signal only when its observable precondition is present, and otherwise preserve the base prompt behavior. Before using object-goal matching, interaction reachability, blocker distance, staged progress, or score_normalized, inspect WINCONDITIONS, RULES, LEGEND aliases/composites, COLLISIONLAYERS, and current object counts. When RULES describe beams, lasers, sensors, wrap, force motion, remote pickup, carrying, pulling, swapping, or other nonstandard movement, do not infer hidden carried-object states, remote interaction distance, or irreversible effects from object names alone; keep base-style target/object matching and player-to-interaction terms unless RULES plus ctx object_positions directly support the stronger term. Keep every estimate finite/nonnegative, use score_normalized only as a low-weight fallback, and never trade base-solved reliability for speculative mechanics terms.
