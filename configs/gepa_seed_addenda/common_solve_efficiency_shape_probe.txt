For common-solve efficiency, preserve cheap move-scale ordering before adding expensive mechanics. In stable pushable-object/target games, use explicit object-target assignment or matching plus bounded player-to-interaction distance so A* can choose the next useful interaction instead of expanding broad count-only or score-only plateaus. Add reachability/BFS only behind observable reasons from RULES, COLLISIONLAYERS, terrain, blockers, hazards, one-way effects, gates, or doors; otherwise prefer Manhattan/matching terms and a tiny score_normalized tie-breaker. When RULES mention push, pull, swap, slide, ice, water/fill, portals, beams, gravity, or other state-changing transitions, model the relevant transition-aware cost instead of replacing it with player-only path distance. Keep all terms finite, nonnegative, and on plausible move scale; use huge penalties only for rule-proven irreversible dead states.
