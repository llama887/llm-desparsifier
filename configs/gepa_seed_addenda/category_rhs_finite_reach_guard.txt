Keep one general prompt. It may use internal categories or if/else branches, but only from WINCONDITIONS, RULES, LEGEND aliases, COLLISIONLAYERS, and current state, never game titles. Every branch term must stay finite and nonnegative; do not use float('inf'), math.inf, nan, or other non-finite sentinels, and clamp the final estimate. Large missing-object, deadlock, or role penalties require the win text to name the role and RULES/aliases/composites/RHS effects to prove the object cannot be created or transformed later; otherwise omit the risky term. For water, fences, walls-as-goals, drop/swap hosts, collapsing terrain, one-way motion, portals, or other blockers, prefer small reachability or legal-interaction-distance terms over pure Manhattan distance or generic counts, and derive composite goals from their component or host relation.
