Keep one general prompt for all games. It may define internal categories or if/else branches, but only from WINCONDITIONS, RULES, LEGEND aliases, COLLISIONLAYERS, and current state properties, never from game titles. Each branch must preserve base robustness: solved states return 0.0, estimates stay finite/nonnegative, uncertain mechanics use reachable interaction distance plus score_normalized fallback, and large deadlock or missing-object penalties require rule-level proof that the condition is irreversible. Before using such a penalty, verify the object type never appears on any RULES right-hand side and cannot be created later. If it can be spawned, use only distance terms or a small score_normalized tie-breaker. Generic role helpers should apply only when win-condition text names that role.
