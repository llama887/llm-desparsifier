For common-solve efficiency, prefer smooth move-scale ranking over large constants: combine subgoal counts with bounded distances or interaction costs so neighboring states do not collapse into broad plateaus. Preserve finite local reachability/path-distance terms only when RULES, COLLISIONLAYERS, terrain, blockers, gates, hazards, or one-way effects make Manhattan distance misleading; otherwise keep cheap matching plus player-to-interaction distance and score_normalized as a low-weight tie-breaker. Preserve code safety while adding those terms: no imports, decorators, collections.deque, float('inf'), math.inf, nan, or non-finite returns; use bounded finite values and plain local lists, indexes, loops, dicts, and sets.
