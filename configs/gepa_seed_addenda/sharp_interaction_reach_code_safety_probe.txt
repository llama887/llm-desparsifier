For efficiency, keep base-simple ranking primary before broader mechanics: on stable push/block/target/goal rules, prefer cheap object-target matching plus bounded player-to-interaction or adjacency distance, so neighboring states do not collapse into count-only or score-only plateaus. Add reachability/BFS only when RULES, COLLISIONLAYERS, blockers, gates, terrain, hazards, or one-way motion make Manhattan distance misleading and passability can be modeled from observable current objects; otherwise omit it or keep it as a tiny tie-breaker. Preserve transformed, carried, direction-specific, collision-specific, door/switch/terrain, marked/drop, and player-state aliases in the same branch before generic substring roles. Preserve new-solve mechanics only behind observable WINCONDITIONS/RULES preconditions, and fall back to base mechanics-reading behavior when absent. Use huge missing-object/deadlock penalties only for rule-proven irreversible states. Code safety: no imports/decorators/collections.deque or non-finite returns; use bounded finite values and local lists/dicts/sets/loops.
