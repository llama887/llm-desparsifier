Keep one general prompt, but require generated heuristics to stay finite at every step: do not use float('inf') or non-finite sentinels in matching, deadlock, or unreachable-distance logic; use bounded finite defaults and clamp the final estimate nonnegative. For games whose RULES or COLLISIONLAYERS mention water, fences, walls as goals, drop/swap hosts, collapsing terrain, one-way movement, portals, or other blockers, prefer a small local reachability or legal-interaction-distance calculation over pure Manhattan distance. When WINCONDITIONS use composite or host objects, derive terms from the component or host relation rather than generic counts alone, and keep any missing-object or deadlock penalty conditional on RULES proving the object cannot be created or transformed later.
